# Extenshi scan report — sample-extension.zip
> Generated by @extenshi/cli v0.1.0 · 2026-07-19T09:00:00.000Z · Job `sample-0000-0000-0000`

## Verdict

**HIGH RISK of store rejection or delisting**

This build has a manifest signal that stores commonly reject or delist for. Fix the high-impact items below before you submit.

## Store-compliance risks (ranked by review danger)

| # | Risk | Severity | Store impact | Stores |
|---|------|----------|--------------|--------|
| 1 | Weakened content security policy | HIGH | REJECTION RISK | CHROME, EDGE |
| 2 | Broad host permissions | MEDIUM | REVIEW_DELAY | CHROME, FIREFOX, EDGE |

### 1. Weakened content security policy

**Why stores care:** extension_pages relaxes the default CSP with 'unsafe-eval', which Chrome Web Store rejects for MV3 submissions.

**Evidence:**

```
extension_pages: script-src 'self' 'unsafe-eval'
```

**How to fix:** Remove 'unsafe-eval' and precompile any dynamic rules at build time.

### 2. Broad host permissions

**Why stores care:** <all_urls> grants read/write access to every site the user visits and triggers a manual review.

**Evidence:**

```
host_permissions: <all_urls>
```

**How to fix:** Narrow to the origins you actually need, or move to the optional_host_permissions + activeTab pattern.

## Manifest risk score

**62/100**

Flags set: `cspWeakened`, `broadHostPermissions`

## Store review prediction (pre-publish)

**1 will be rejected · 1 cause user attrition · 2 trigger slow review**

### Will be rejected (1)

#### CSP allows 'unsafe-eval'

Manifest V3 forbids remotely-hosted and dynamically-evaluated code. A manifest that relaxes script-src with 'unsafe-eval' is rejected automatically, before a human reviewer looks at the listing.

**Found:** `manifest.json → content_security_policy.extension_pages: script-src 'self' 'unsafe-eval'`

**How to fix:** Drop 'unsafe-eval' and compile the rules engine ahead of time, or ship a WASM/JSON interpreter instead of eval().

_Chrome Web Store — Manifest V3 remote code policy_

### User attrition on update (1)

#### Update adds a permission that disables the extension for existing users

Adding "history" to an already-published extension puts it in a disabled state until every existing user manually re-grants the permission. Historically this costs a large share of the active install base.

**Found:** `permissions: +history`

**How to fix:** Move "history" to optional_permissions and request it at runtime from the feature that needs it.

_Chrome — permission warnings on update_

### Triggers slow review (2)

#### <all_urls> host permission triggers manual review

Extensions requesting access to all sites are routed to a manual reviewer, typically adding days to the review queue.

**Found:** `host_permissions: <all_urls>`

**How to fix:** Request specific origins, or use activeTab for click-driven access.

_Chrome Web Store — review times_

#### Large minified vendor bundle without a readable source

A 41 MB minified bundle with no source map slows down human review and frequently draws a request for reviewable source.

**Found:** `vendor/analytics.bundle.js (41 MB, minified, no source map)`

**How to fix:** Ship a source map or an unminified build alongside the artifact.

_Chrome Web Store — code readability requirements_

_Manifest diff against the last published version was skipped — pass --extension-id to enable it._

## Security findings

### CRITICAL (3)

| Finding | Scanner | Location |
|---------|---------|----------|
| eval-usage | semgrep | vendor/analytics.bundle.js:1842 |
| eval-usage | semgrep | vendor/analytics.bundle.js:2907 |
| eval-usage | semgrep | src/background/rules-engine.js:64 |

### HIGH (3)

| Finding | Scanner | Location |
|---------|---------|----------|
| innerhtml-assignment | semgrep | src/content/panel.js:212 |
| innerhtml-assignment | semgrep | src/content/tooltip.js:47 |
| remote-code-load | static-scan | src/background/loader.js:33 |

### MEDIUM (2)

| Finding | Scanner | Location |
|---------|---------|----------|
| http-endpoint | semgrep | src/background/sync.js:18 |
| broad-host-permission | static-scan | manifest.json |

### LOW (1)

| Finding | Scanner | Location |
|---------|---------|----------|
| high-entropy-identifiers | obfuscation-detector | vendor/analytics.bundle.js |

## Scanner status

5/6 scanners succeeded.

| Scanner | Status | Detail |
|---------|--------|--------|
| app-inspector | failed | scanner timed out after 180s on a 41 MB bundle |

## Notes

- Scan job: `sample-0000-0000-0000`
- This scan is automated. The scanners are heuristics that may miss real issues or over-flag benign code — a clean result is not a guarantee of safety or store approval. See the [security-risk disclaimer](https://catalog.extenshi.io/disclaimers/security-risk).
- If a credit was charged for a failed scan it auto-refunds within a minute.

